import Foundation

/// Version marker for the import package. Bump it when the shape of what the importers
/// produce changes, so a build can be identified from the outside.
public enum CenitImport {
    public static let version = "0.1.0"
}
